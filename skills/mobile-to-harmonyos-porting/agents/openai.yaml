interface:
  display_name: "Mobile to HarmonyOS Porting"
  short_description: "Stage exact-fidelity mobile ports to HarmonyOS"
  default_prompt: "Use $mobile-to-harmonyos-porting to bootstrap a HarmonyOS target if needed, then migrate this source snapshot with every visible source element exact before each function category."

policy:
  allow_implicit_invocation: true
