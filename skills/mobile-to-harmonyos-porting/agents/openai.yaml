interface:
  display_name: "Mobile to HarmonyOS Porting"
  short_description: "Stage page UI, then functional ports to HarmonyOS"
  default_prompt: "Use $mobile-to-harmonyos-porting to migrate this source snapshot page by page, then function category by function category."

policy:
  allow_implicit_invocation: true
